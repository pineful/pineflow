{
  "name": "pineflow-infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "synth": "cdk synth",
    "verify": "npm run build && npm run synth && node scripts/verify-template.mjs",
    "diff": "cdk diff",
    "deploy": "cdk deploy"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.232.1",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^24.10.2",
    "aws-cdk": "^2.1034.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
